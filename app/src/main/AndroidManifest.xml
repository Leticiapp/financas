<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.financas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Financas">
        <activity
            android:name=".activity.SplashScreenActivity"
            android:label="@string/app_name"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Financas.NoActionBar">
        </activity>
        <activity
            android:name=".activity.Cadastro"
            android:label="@string/app_name"
            android:theme="@style/Theme.Financas.NoActionBar">
        </activity>
        <activity android:name=".activity.Extrato" />
        <activity android:name=".activity.Pesquisar" />
        <activity android:name=".activity.ListaClassificada"/>
        <activity android:name=".activity.PesquisaResultado"/>

    </application>

</manifest>